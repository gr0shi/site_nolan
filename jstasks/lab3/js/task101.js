function foo () {
    var month = 6;
    if (month == 12 || month <= 2) {
    alert( 'Зима' );
    }
    if (month >= 3 && month <= 5) {
    alert( 'Весна' );
    }
    if (month >= 6 && month <= 8) {
    alert( 'Лето' );
    }
    if (month >= 9 && month <= 11) {
    alert( 'Осень' );
    }
}